import * as legacy from "@/lib/three-card-poker";

export const DEFAULT_RULESET = legacy.DEFAULT_RULESET as typeof legacy.DEFAULT_RULESET;
export const HAND_RANKS = legacy.HAND_RANKS as typeof legacy.HAND_RANKS;
export const HAND_LABELS = legacy.HAND_LABELS as typeof legacy.HAND_LABELS;

